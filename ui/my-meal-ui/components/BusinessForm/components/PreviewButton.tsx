import { Button } from "@mui/material";

const PreviewButton = () => {
  return (
    <Button variant="contained" color="primary">
      Vista Previa
    </Button>
  );
};

export default PreviewButton;
